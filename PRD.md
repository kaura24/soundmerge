# Sound Forge MVP 제품 요구사항

## 1. 제품 정의

Sound Forge는 기본적으로 MP3 한 개와 이미지 또는 짧은 동영상 한 개를 결합해 YouTube 업로드용 MP4를 만드는 macOS Electron 앱이다. 선택적으로 여러 개의 이미지·오디오 쌍을 순서대로 연결하는 Multi-Pair 모드도 제공한다.

주 사용자는 영상 편집 도구나 FFmpeg 명령을 다루지 않고도 음악 전체 길이에 맞는 영상을 자동으로 만들 수 있어야 한다.

## 2. MVP 목표

사용자는 다음 흐름만으로 결과를 만들 수 있어야 한다.

1. MP3 한 개를 선택한다.
2. 이미지 또는 동영상 한 개를 선택한다.
3. 앱 안에서 음악 전체 길이의 결합 결과를 미리 본다.
4. 저장 폴더와 파일명을 선택한다.
5. YouTube용 MP4를 생성한다.
6. 앱에서 결과를 재생하거나 Finder에서 연다.

기본 흐름은 Single Pair 모드이며, Multi-Pair 모드에서는 이미지·오디오 쌍을 여러 개 추가하고 순서를 바꾼 뒤 하나의 MP4로 연결한다.

최종 MP4 길이는 MP3 길이와 같아야 한다. 동영상 입력이 음악보다 짧으면 처음부터 끝까지 반복하고, 마지막 반복분은 음악 종료 시점에서 자른다.

## 3. 지원 플랫폼과 배포

- macOS 전용 MVP
- Apple Silicon과 Intel Mac을 모두 지원하는 Universal 앱
- Electron 데스크톱 앱
- FFmpeg와 필요한 실행 파일을 앱에 포함
- 사용자는 Homebrew, FFmpeg 또는 별도 런타임을 설치하지 않아야 함

Windows와 Linux는 MVP 범위에서 제외한다.

## 4. 입력

Single Pair 모드에서는 한 작업에서 다음 파일을 각각 한 개만 사용한다.

### 오디오

- MP3
- MP3에 첨부된 커버 이미지가 있으면 GUI에서 원본 내장 artwork를 시각 자료로 사용할 수 있다.
- 내장 artwork 추출은 원본 그림 스트림을 재인코딩하지 않고 임시 파일로 복사한다.

### 시각 자료

- 이미지: JPG, JPEG, PNG
- 동영상: MP4 컨테이너의 H.264 영상

입력 동영상에 오디오 스트림이 있더라도 완전히 무시한다. 최종 MP4의 오디오는 선택한 MP3에서만 생성한다.

GIF, WebP, MOV, AVI 및 기타 형식은 MVP에서 지원하지 않는다.

Multi-Pair 모드에서는 한 개 이상의 쌍을 사용할 수 있으며 최대 개수 제한은 없다. 각 쌍마다 MP3 한 개와 JPG/JPEG/PNG 또는 H.264 MP4 한 개를 사용한다. 쌍 사이의 순서는 사용자가 정하며, 각 오디오 길이만큼 해당 시각 자료를 유지한 뒤 다음 쌍으로 연결한다.

## 5. 미리보기

MP4를 생성하기 전에 앱 안에서 다음을 확인할 수 있어야 한다.

- MP3와 시각 자료가 결합된 상태로 재생
- 이미지가 음악 전체 길이 동안 유지되는 결과
- 짧은 동영상이 음악 길이에 맞춰 반복되는 결과
- 음악 전체 길이의 타임라인
- 재생 및 일시정지
- 타임라인 위치 이동
- 현재 위치와 전체 길이 표시

미리보기는 전체 MP4를 먼저 렌더링하지 않고 사용할 수 있어야 한다. 미리보기의 반복 방식, 화면 배치, 종료 시점은 최종 산출물과 일치해야 한다.

## 6. 최종 영상 규격

YouTube 업로드용 고정 프리셋을 사용한다.

| 항목 | 값 |
| --- | --- |
| 컨테이너 | MP4 |
| 해상도 | 1920×1080 |
| 화면비 | 16:9 |
| 프레임레이트 | 30fps progressive |
| 영상 코덱 | H.264 High Profile |
| 픽셀 포맷 | yuv420p |
| 영상 비트레이트 | 약 8 Mbps VBR |
| 오디오 코덱 | AAC-LC |
| 오디오 | 48 kHz, Stereo, 384 kbps |
| 색공간 | BT.709 SDR |
| 웹 재생 최적화 | MP4 Fast Start |

참고:

- [YouTube 권장 업로드 인코딩 설정](https://support.google.com/youtube/answer/1722171)
- [YouTube 해상도 및 화면비율](https://support.google.com/youtube/answer/6375112)

## 7. 화면 배치

- 16:9 입력은 1920×1080 화면을 채운다.
- 세로형, 정사각형 또는 기타 화면비 입력의 원본 전경은 잘라내거나 늘리지 않는다.
- 남는 영역은 같은 입력을 화면 비율에 맞게 확대·크롭한 배경으로 채운다.
- 검은 레터박스 또는 필러박스를 결과에 직접 넣지 않는다.
- 이미지와 동영상에 같은 화면 배치 원칙을 적용한다.

## 8. 산출물 길이와 반복 규칙

- 최종 길이의 기준은 MP3의 실제 재생 길이다.
- 이미지 입력은 MP3 전체 길이 동안 한 장을 유지한다.
- 동영상 입력은 원본의 처음부터 끝까지 순서대로 반복한다.
- 마지막 반복분은 MP3 종료 시점에서 자른다.
- 최종 MP4에는 영상 스트림 한 개와 오디오 스트림 한 개가 있어야 한다.
- 컨테이너 및 코덱 타임스탬프 정밀도를 고려해 자동 검증에서는 MP3와 MP4 길이 차이를 최대 0.05초까지 허용한다.

## 9. 저장과 결과 확인

- 생성 전에 macOS 저장 대화상자에서 폴더와 파일명을 선택한다.
- 출력 확장자는 `.mp4`를 사용한다.
- 기존 파일은 사용자 확인 없이 덮어쓰지 않는다.
- 생성 완료 후 앱 안에서 결과 MP4를 재생할 수 있다.
- `Finder에서 보기` 기능을 제공한다.
- 변환 중에는 단순 작업 중 상태를 표시한다.

진행률, 예상 남은 시간, 변환 취소는 MVP에서 제공하지 않는다.

## 10. 오류 처리

다음 상황에서는 생성하지 않고 GUI에 이해 가능한 오류를 표시한다.

- 입력 파일이 선택되지 않음
- 지원하지 않는 확장자 또는 코덱
- 파일을 읽거나 디코딩할 수 없음
- 출력 경로에 쓸 수 없음
- FFmpeg 실행 또는 인코딩 실패
- 디스크 공간 부족 등으로 출력 파일을 완성하지 못함

실패한 출력 파일을 정상 결과처럼 표시하지 않는다.

## 11. MVP 제외 범위

- 서로 다른 작업의 일괄 변환
- 슬라이드쇼 및 여러 영상 연결
- 일괄 변환
- 음악·영상 구간 자르기
- 시작 위치 조정
- 볼륨 조정 및 페이드
- 자막, 텍스트, 로고, 워터마크
- 전환 효과, 필터, 색보정
- 반복 구간 수동 편집
- YouTube 계정 연결 및 자동 업로드
- Windows 및 Linux
- 자동 업데이트
- 진행률, 예상 시간, 변환 취소
- 보안 강화 작업
- 테스트 샘플 생성 프로그램

## 12. 테스트 자료 정책

- 테스트용 MP3, 이미지, 동영상은 반드시 인터넷에서 내려받은 실제 파일을 사용한다.
- 테스트 샘플을 생성하거나 길이를 만들기 위한 별도 프로그램을 작성하지 않는다.
- 다운로드한 샘플을 테스트 조건에 맞추기 위해 인위적으로 생성하거나 잘라내지 않는다.
- 사용 출처 URL과 라이선스를 기록한다.
- 공개 도메인, CC0 또는 테스트 사용이 명확히 허용된 자료를 우선한다.
- 합격 테스트에는 길이가 4분 이상인 MP3를 반드시 포함한다.
- 서로 다른 길이의 짧은 인터넷 동영상을 사용해 반복 처리를 확인한다.
- 이미지 입력과 동영상 입력을 각각 검증한다.

## 13. 필수 합격 기준

### GUI

- 지원 입력 파일을 GUI에서 선택하고 식별할 수 있다.
- 내장 artwork가 있는 MP3는 별도 이미지 파일 없이 커버 사용 옵션을 제공한다.
- 결합 결과를 생성 전에 전체 타임라인으로 재생하고 탐색할 수 있다.
- 저장 위치와 파일명을 GUI에서 선택할 수 있다.
- 완료된 MP4를 앱에서 재생할 수 있다.
- Finder에서 결과 위치를 열 수 있다.

### 이미지 입력 결과

- 인터넷에서 받은 4분 이상 MP3와 인터넷 이미지를 사용한다.
- 출력 길이는 MP3 길이와 0.05초 이내로 같다.
- 영상이 전체 재생 시간 동안 유지된다.
- 출력에 영상과 오디오가 모두 존재한다.

### 동영상 입력 결과

- 인터넷에서 받은 4분 이상 MP3와 짧은 인터넷 동영상을 사용한다.
- 짧은 영상이 MP3 종료 시점까지 순서대로 반복된다.
- 마지막 반복은 MP3 종료 시점에서 끝난다.
- 입력 동영상의 오디오는 결과에 포함되지 않는다.
- 출력 길이는 MP3 길이와 0.05초 이내로 같다.

### Multi-Pair 결과

- 한 개 이상의 이미지·오디오 쌍을 GUI에서 추가할 수 있고 최대 개수 제한이 없다.
- 쌍의 순서를 위·아래로 변경하거나 개별 쌍을 제거할 수 있다.
- 각 쌍의 오디오 길이만큼 순서대로 연결한 단일 MP4를 생성한다.
- Multi-Pair에서도 영상 스트림 한 개와 오디오 스트림 한 개의 출력 규격을 유지한다.

### 기술 규격

`ffprobe` 또는 동등한 검사로 다음을 자동 검증한다.

- MP4 컨테이너
- H.264 영상 스트림 한 개
- AAC 오디오 스트림 한 개
- 1920×1080
- 30fps
- yuv420p
- 48 kHz Stereo
- 총 길이 240초 이상인 합격 산출물
- 일반 macOS 플레이어와 앱 내부 플레이어에서 재생 가능

### 배포

- Apple Silicon Mac에서 앱이 실행된다.
- Intel Mac에서 앱이 실행된다.
- 두 아키텍처 모두 별도 FFmpeg 설치 없이 이미지 및 동영상 변환이 가능하다.

## 14. 구현 결정 권한

아래 항목은 이 PRD의 사용자 경험과 합격 기준을 바꾸지 않는 범위에서 구현 단계에서 결정할 수 있다.

- Electron 내부 구조와 UI 프레임워크
- 미리보기 동기화 방식
- FFmpeg 명령 구성과 H.264 인코더 선택
- Universal 앱 패키징 방식
- 임시 파일 관리 방식
- 내부 상태 모델과 테스트 프레임워크
- 세부 시각 디자인과 문구

다음 변경은 요구사항 변경으로 취급한다.

- 출력 규격 또는 길이 규칙 변경
- 미리보기 축소 또는 제거
- 지원 형식 확대
- 편집 기능 추가
- Windows/Linux 추가
- 여러 파일 또는 일괄 처리 추가

## 15. 현재 위험과 선행 확인

- Universal Electron 앱에 포함할 FFmpeg의 arm64/x64 실행 검증이 필요하다.
- FFmpeg 배포 라이선스와 선택한 H.264 인코더의 배포 조건을 확인해야 한다.
- 긴 전체 타임라인 미리보기에서 오디오와 반복 영상의 동기화 안정성을 검증해야 한다.
- 현재 저장소 운영 규칙의 Mac/Windows 동시 릴리즈 조건과 macOS 전용 MVP 범위를 구현 전에 일치시켜야 한다.
- Google Drive 프로젝트 내부에 의존성, 캐시, 빌드 및 원시 테스트 산출물을 만들지 않도록 외부 `LOCAL_ROOT`를 확정해야 한다.

## 16. 현재 구현 현황 (2026-07-25)

- Single Pair와 Multi-Pair(1:1) 모드가 모두 구현되어 있다.
- 최근 Studio형 화면은 제거하고 기존 Sound Forge 마스터링 데스크 디자인으로 롤백했으며 Multi-Pair 기능은 유지했다.
- `Reset session`과 앱 내 전체 길이 미리보기·출력 재생이 구현되어 있다.
- MP3 선택 시 첨부 artwork 존재 여부를 표시하고, 단일 작업과 각 Multi-Pair에서 원본 artwork를 시각 자료로 선택할 수 있다.
- Artwork 추출 파일은 macOS 임시 경로에만 저장하며 Drive 프로젝트에는 생성하지 않는다.
- x86_64 Intel 실행과 Universal 패키지 구조를 확인했다. Apple Silicon 실기기 실행, 서명·공증은 아직 남아 있다.

## 16. 현재 구현 현황 (2026-07-25)

- Single Pair MVP의 입력 선택, 전체 타임라인 미리보기, 영상 반복, 4분 이상 MP4 렌더링, 결과 재생을 구현하고 실제 인터넷 샘플로 검증했다.
- Multi-Pair 모드의 쌍 추가·삭제·순서 변경·순차 합성을 구현했으며, 관련 단위 테스트를 포함해 총 21개 단위 테스트가 통과했다.
- 화면은 최근의 Studio형 시안을 제거하고, 기존 Sound Forge 마스터링 데스크 디자인으로 롤백했다. Multi-Pair 기능용 탭과 페어 목록만 기존 디자인 언어에 맞춰 추가했다.
- Reset session과 저장 대화상자 확인 후 기존 출력 교체를 지원한다.
- Universal 앱 바이너리는 생성됐고 Intel Mac 실행을 확인했다. Apple Silicon 실기기 실행·렌더링, 서명/notarization, AAC 실측 비트레이트 정책은 출시 전 남은 작업이다.
